# macOS
.DS_Store
.AppleDouble
.LSOverride

# Claude Code settings
.claude/

# Large reference / design files
JGS Living Landing (standalone).html
"JGS Living Landing (standalone).html"

# PDF mockups (optional — remove these lines if you want PDFs tracked)
*.pdf

# Node (if ever used)
node_modules/

# Internal documentation (not for deployment)
/*.md
!/README.md

# Design agent working files (not for deployment)
.agents/
**/.agents/
